export interface Portfolio {
    header: string;
    description: string;
    images: any[];
    timestamp: any;
  }
  
  export interface PortfolioData{
      id: string;
      data: Portfolio;
      
  }

